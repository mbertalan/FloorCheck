# Packaged builds (25 MB each; they live on ficsit.app)
releases/
*.zip

# Unreal build output, if the plugin is ever built inside this folder
Binaries/
Intermediate/
Saved/
DerivedDataCache/
.vs/
*.pdb

# Session pickup notes, not part of the mod
NEXT_SESSION.md

# Raw game screenshots (the web-sized copies live in docs/screenshots/)
/Screenshots/
